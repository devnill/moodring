id: WI-001
type: work_item
title: Plugin Scaffold
status: pending
complexity: low
scope:
  - {path: .claude-plugin/plugin.json, op: create}
  - {path: settings.json, op: create}
  - {path: hooks/, op: create}
  - {path: scripts/, op: create}
  - {path: skills/, op: create}
  - {path: sounds/, op: create}
depends: []
blocks:
  - "004"
  - "007"
  - "009"
criteria:
  - File `.claude-plugin/plugin.json` exists with `name` = `"moodring"`, `version` = `"1.0.0"`
  - "File `settings.json` exists with keys: `muted` (false), `poll_interval` (\"5m\"), `volume` (0.2), `player` (\"\")"
  - Directory `hooks/` exists
  - Directory `scripts/` exists
  - Directory `skills/` exists
  - Directory `sounds/` exists
  - "`plugin.json` contains `\"hooks\": \"./hooks/hooks.json\"`"
module: null
domain: null
notes: |
  # Implementation Notes: 001
  
  ### plugin.json
  ```json
  {
    "name": "moodring",
    "description": "Sonifies an AI model's self-reported internal state by playing mood-mapped synthesized sounds",
    "version": "1.0.0",
    "author": { "name": "dan" },
    "license": "MIT",
    "hooks": "./hooks/hooks.json"
  }
  ```
  
  ### settings.json
  ```json
  {
    "muted": false,
    "poll_interval": "5m",
    "volume": 0.2,
    "player": ""
  }
  ```
  
  The plugin root is the directory containing `.claude-plugin/`. All paths in hooks and scripts use `${CLAUDE_PLUGIN_ROOT}` to reference files relative to this root.
cycle_created: 1
cycle_modified: null
content_hash: cfef39a40fa85e414ab6271932503cf5004c0cc58be35a0a0fe46a98102af07e
token_count: 384
file_path: .ideate/work-items/WI-001.yaml
